

<section class="mx-20" >
<%= render "devise/shared/logout" %>
  <% @users.each do |user| %>
    <a href="/users/<%= user.id%>" class="user">
      <img class="pro-pic" src='<%= user.photo%>' alt='Profile-pic'>
      <div class="user-dets">
        <h2 class="user-name" ><%=user.name%></h2>
        <p class='num-posts' >Number of Posts: <%=user.posts_counter%></p>
      </div>
    </a>
  <%end%>
</section>